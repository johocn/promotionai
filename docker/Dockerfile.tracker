# docker/Dockerfile.tracker
FROM node:18-alpine

WORKDIR /app

# 复制应用代码
COPY src/tracker/ ./src/tracker/
COPY config/ ./config/

# 安装tracker依赖
RUN cd src/tracker && npm install --production

# 创建日志目录
RUN mkdir -p logs

EXPOSE 3000

CMD ["sh", "-c", "cd src/tracker && npm start"]