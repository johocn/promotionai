# docker/Dockerfile.tracker
FROM node:18-alpine

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制应用代码
COPY src/tracker/ ./src/tracker/
COPY config/ ./config/

# 创建日志目录
RUN mkdir -p logs

EXPOSE 3000

CMD ["npm", "start"]